- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Development

- shell:
    -
        command: ./setup_homebrew.zsh
        stdout: true
        stderr: true